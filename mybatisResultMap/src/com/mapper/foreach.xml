<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.foreach">
    <select id="queryForeach" resultMap="com.domain.Mybatisforeach" parameterType="Integer[]">
       select * FROM  Mybatisforeach where  id

            <foreach collection="array" item="a1" open="in (" close=" )" separator=",">
               #{a1}
            </foreach>

    </select>
</mapper>